# Live Evidence Feeds

Run `scripts/refresh.sh` to generate atomic PyPI and npm evidence snapshots. Generated JSON is local runtime evidence and is ignored by Git. `brain.sh doctor` reports malformed or stale snapshots.
