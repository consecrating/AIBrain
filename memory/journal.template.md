# Session Journal

> Append-only log of significant events. Never edit past entries.
> Format: [timestamp] [category] description
>
> ⚠️ This file is a TEMPLATE. The live copy (`journal.md`) is gitignored.
> `brain.sh decide` and `brain.sh correct` both append here automatically, so
> the live file accumulates project and client detail. Keep it private, and
> never record credentials — use the private vault instead.

## YYYY-MM-DD

- [YYYY-MM-DDTHH:MM] [INIT] Brain initialised
