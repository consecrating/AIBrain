# Ingested References

`brain.sh ingest FILE` writes fingerprinted local reference documents here. Generated references are ignored by Git because they can contain private or licensed source material. Commit an individual reference only after reviewing its content and provenance.
